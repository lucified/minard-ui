import * as React from 'react';

import { Activity } from '../../../modules/activities';

interface Props {
  activity: Activity;
  showProjectName?: boolean;
}

const CommentActivity = (_props: Props) => {
  // TODO
  return <span />;
};

export default CommentActivity;
