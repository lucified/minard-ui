machine:
  node:
    version: 6.9
  environment:
    PROJECT: $CIRCLE_PROJECT_REPONAME
    BRANCH: $CIRCLE_BRANCH
    COMMIT: $CIRCLE_SHA1

test:
  override:
    - npm run compiled-test

deployment:
  chatops:
    branch: /^(?!nodeploy-).*$/
    commands:
      - npm run deploy-chatops
